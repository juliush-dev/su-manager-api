title: Role
type: object
x-tags:
  - enumeration
description: Represents the role of a new created user.
properties:
  value:
    type: string
    example: can-validate-account
    enum:
      - can-validate-account
      - can-block-account
      - can-delete-account
      - no-role
    readOnly: true
required:
  - value
